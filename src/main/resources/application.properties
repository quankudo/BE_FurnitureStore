spring.application.name=furniture-store

# Connect to MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/FurnitureStore
spring.datasource.username=root
spring.datasource.password=root

server.servlet.context-path=/api/v1.0

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

jwt.signerKey=${JWT_SECRET}
jwt.valid-duration=3600
jwt.refreshable-duration=36000

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=quannguyen2004aa@gmail.com
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

cloudinary.cloud-name=${CLOUDINARY_NAME}
cloudinary.api-key=${CLOUDINARY_KEY}
cloudinary.api-secret=${CLOUDINARY_SECRET}
